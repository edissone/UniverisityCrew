spring:
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.HSQLDialect
    database: H2
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:university_db
    username: sa
    password: sa